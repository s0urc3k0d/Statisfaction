########################################
# Application / Serveur HTTP
########################################
# Port HTTP du serveur Express
PORT=4100
# Secret de session (changez-le en production)
SESSION_SECRET=dev_super_secret_change_me
# Base URL publique du serveur (utile pour EventSub callbacks)
BASE_URL=http://localhost:4100
# Domaine public (utilisé dans certains liens absolus / emails)
PUBLIC_SITE_URL=https://statisfaction.sourcekod.fr
# URL du frontend (CORS + redirections)
FRONTEND_URL=http://localhost:3100

########################################
# Base de données Prisma (SQLite par défaut)
########################################
DATABASE_URL=file:./dev.db

########################################
# Sessions (optionnel, recommandé en prod)
########################################
# URL Redis pour stocker les sessions (ex: redis://localhost:6379)
#REDIS_URL=redis://localhost:6379
# Forcer le cookie de session en "secure" (true en prod derrière HTTPS)
#COOKIE_SECURE=true

########################################
# Twitch API (OAuth + EventSub)
########################################
TWITCH_CLIENT_ID=your_twitch_client_id
TWITCH_CLIENT_SECRET=your_twitch_client_secret
# URI de redirection OAuth (doit correspondre à la config Twitch dev console)
TWITCH_REDIRECT_URI=http://localhost:4100/auth/twitch/callback

# Secret pour signer les webhooks EventSub (obligatoire pour activer EventSub)
#TWITCH_EVENTSUB_SECRET=choose_a_long_random_secret
# URL publique de callback EventSub (exposée via tunnel en dev)
#TWITCH_EVENTSUB_CALLBACK_URL=https://your-public-url/webhooks/twitch/eventsub

########################################
# Email SMTP (pour l’envoi du récap par mail)
########################################
# Hôte SMTP (ex: smtp.gmail.com, mailhog=localhost)
#SMTP_HOST=
# Port SMTP (25, 465=SSL, 587=STARTTLS)
#SMTP_PORT=
# Connexion SSL directe (true/false)
#SMTP_SECURE=false
# Identifiants SMTP (si requis)
#SMTP_USER=
#SMTP_PASS=
# Adresse d’expéditeur
#MAIL_FROM="Statisfaction <no-reply@example.com>"
